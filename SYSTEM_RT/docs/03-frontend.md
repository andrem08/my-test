# Frontend

## Estado Atual do Repositorio
Nao existe frontend implementado neste repositorio.

## Itens Solicitados (rotas, stores, API client, boot files, quasar.config)
- Rotas frontend: inexistentes.
- Stores (Pinia/Vuex/Redux): inexistentes.
- API client frontend: inexistente.
- Boot files (Quasar): inexistentes.
- `quasar.config`: inexistente.

## Impacto
A integracao com este backend e feita por clientes externos (scripts, testes HTTP, possivel frontend em outro repositorio) consumindo endpoints Flask.

## Recomendacao de documentacao cruzada
Se houver frontend em outro repo, manter neste projeto um link para:
- URL do repositorio frontend;
- versao de contrato de API consumida;
- matriz de compatibilidade backend x frontend.
